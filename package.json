{
  "name": "@shevelidze/easyrest",
  "version": "0.1.5",
  "description": "A library for RESTing without a headache.",
  "directories": {
    "test": "test"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "jest --verbose",
    "build": "tsc",
    "bundle": "npm test && npm run build && npm publish --access=public"
  },
  "author": "shevelidze",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/shevelidze/easyrest.git"
  },
  "devDependencies": {
    "@types/jest": "^28.1.1",
    "jest": "^28.1.1",
    "ts-jest": "^28.0.5",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "jtd": "^0.1.1"
  }
}
