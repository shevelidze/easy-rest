import { EntityMember } from './entityMembers';
import EntityObject from './EntityObject';

export default interface ArrayObject {
  arrayEntityMemberName: string;
  ownerEntityObject: EntityObject;
}
